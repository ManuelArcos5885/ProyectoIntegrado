import { ComentarioConNombreUsuario } from './comentario-con-nombre-usuario';

describe('ComentarioConNombreUsuario', () => {
  it('should create an instance', () => {
    expect(new ComentarioConNombreUsuario()).toBeTruthy();
  });
});
