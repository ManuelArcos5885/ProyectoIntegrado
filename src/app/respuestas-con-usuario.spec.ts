import { RespuestasConUsuario } from './respuestas-con-usuario';

describe('RespuestasConUsuario', () => {
  it('should create an instance', () => {
    expect(new RespuestasConUsuario()).toBeTruthy();
  });
});
